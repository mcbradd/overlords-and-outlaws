> **Superseded candidate - player correction, 17 September 2026:** The user rejected unrestricted Court entry: foreign cards must enter through marriage. Trade may request only a rival's face-up Played/Resting cards. The any-card Add / no-marriage candidate below is historical planning evidence and must not be implemented as written. See [the current correction and regression tasks](reviews/BUILD-5-PLAYER-FOLLOWUP-2026-09-17.md). Its dependent tutorial, state and acceptance package requires redesign before implementation.

# Build 6 production plan

17 September 2026. **Frozen for the next implementation phase; no Build 6 implementation performed.** This plan, complete rules, state boundary, explicit test contract, literal visual inventory, tutorial contract and normative-document cascade are the preimplementation package. The user has requested stopping at this boundary and leaving [a knowledge-base handoff](knowledge-base/build-6-handoff/README.md). A later resumed implementation uses the existing autonomous/Prod authorization without asking for it again.

## Decision and evidence

Implement the single candidate in [complete rules](BUILD-6-RULES-CANDIDATE.md): mixed Courts, three neighboring ranks held to the next turn, same-suit Swap, neighboring-rank Answer, individual Resting return, refill to two and mandatory one-card play. All three card operations depend on specific ranks/suits; there is no global action toolbar, voluntary Pass or external clock. This is an evaluation candidate, not a claim of superior balance.

The [completed Build 5 cohort](reviews/BUILD-5-CYCLE-1-FEEDBACK.md) contains ten observed ordinary games, eight wins/two losses, and a failed family-comprehension assessment. Preserve its meaningful conditional defense, recovery and public-information decisions while replacing the lengthy succession, trade and pass procedures. The [whole-game gauntlet](reviews/BUILD-5-WHOLE-GAME-GAUNTLET.md) rejects unassailable top-rank runs and neighbor-only attacks with excessive sparse-opening forced passes. Native-only run building was rejected for a circulation lock. Malachy's full email and all-mechanics-open instruction guided these decisions; history remains a theme and evidence source, not a reason to preserve an unclear mechanic.

The published control is `7e521f8b29d704291f00af3a9db245fddae00b94`. Its complete release run failed 23 of 63 checks. All 2,228 captured matrix images were actually reviewed; the failed/missing portions and native-size limitations remain open. None of that evidence automatically approves new code or images.

## Scope and public test boundaries

[State and transition contract](BUILD-6-STATE-CONTRACT.md) fixes schema 6, action names, pending custody, public projections, ordered movement events and input/storage boundaries. It is part of the preimplementation freeze.

Keep a physical 3D card-and-board prototype, 63:88 reference faces, original source art, deterministic rules, private hands, local handoff, keyboard/touch, inspection and reduced motion. The next evaluation deploy will receive its actual sequential build number from the release workflow; this plan calls it Build 6, but must report the deployed number rather than assume it.

Test boundaries are the existing public engine operations (`createGame`, `applyAction`, `legalActions`, `viewForSeat`, invariant validation), public AI decision function receiving a seat view, storage import/export/load boundary, generated card/reference output and real rendered browser interactions. Observe source/target/result and accessible controls through the app. Mock only genuine external failures such as denied storage, loading and time; do not mock the reducer, renderer, content or other owned modules to manufacture passes. The user's human-out-of-the-loop instruction governs seam selection; record these choices here instead of interrupting for approval.

All known task/test criteria are written before implementation. Execute meaningful red/green slices at these boundaries, retaining the failing observation before a fix. Newly discovered work needs a written task/test amendment before editing that behavior. Tests must use independently worked examples; neither tests nor screenshots may declare themselves reviewed.

## Complete work breakdown

Every task requires all its listed acceptance criteria before it is marked done. A blocked or limited item remains explicit. The [explicit test contract](BUILD-6-TEST-CONTRACT.md), [tutorial contract](BUILD-6-TUTORIAL-CONTRACT.md) and literal visual inventory are part of each applicable row, not optional supplements.

| ID | Deliverable | Required tests and acceptance |
|---|---|---|
| B601 | Freeze one coherent rules/design/production package. Archive the prior normative Build 5 rules and update suit/rank rules, master specification, session record, creative direction, tutorial contract and current-rule pointers. | No active document simultaneously requires old seals, native-only Add, succession, higher-card defense, special Trade deployment, voluntary Pass or round cap. Every removed mechanism has a decision and replacement coverage. Complete action, response, return, refill, win, empty and ownership rules agree. Every task below has tests; literal inventory count/hash is stored before source implementation. |
| B602 | Replace the core state/reducer with schema 6 and the selected complete game. | Explicit engine cases cover all 13 cyclic runs and neighbor relations, mixed suits, duplicate ranks, all Add/Swap/Answer outcomes, cancellation/rejection, turn order, several Crowns, first-turn two cards, return-before-refill, larger hands, last draw, empty supply/seats and natural all-empty draw. Every transition conserves each selected identity exactly once. Public views and invalid actions reveal no secret identities. Terminal states reject further actions. |
| B603 | Implement an opponent that evaluates public threats and conditional resources. | Same public view/own hand gives identical choices despite different hidden opponent cards/deck order. Paired known-threat fixtures reverse Add/offer/answer preferences where worked outcomes justify it. Compare compulsory exposure, accepting useful donated ranks, overlapping runs and upcoming opponent wins. Record full continuations, not only a preferred-action assertion. Repeated losing known attacks must trigger a meaningful alternative when available. Run fixed 2/3/4-player policy comparisons; do not use aggregate win rate as proof of agency. |
| B604 | Version storage and recovery without reinterpreting old games. | Schema 6 round-trips ordinary, pending-response, multi-Crown and terminal states. Old Build 5 data remains untouched under its old namespace and is rejected with a concise explanation if imported; do not silently turn an old succession into a rank run. Invalid JSON/schema/identities, partial saves, stale revisions, denied storage and interrupted writes preserve an existing valid game. Export remains usable and errors are visible/announced. |
| B605 | Revise shared card/reference art and printed aids for the new game. | All 52 identities retain exact portraits, full names, suit and rank; all live and reference faces are 63:88 with matching stock/frame corners, full portrait coverage and legible indices. No obsolete Recruit/Queen-power/Trade/succession text. Show Add/Swap/Answer from cards; a physical ring aid and transfer example carry the common rules with minimal text. Actually open every face/reference and shared visual asset; record native readable scale, original hashes and defects. No original artwork is overwritten. |
| B606 | Diagnose and repair the real 3D renderer, then implement deliberate mixed-Court placement and Crown presentation. | Reproduce the prior striped portrait through the real renderer and open the baseline. Record ranked falsifiable hypotheses, change one variable per probe, and preserve before/after captures. Cards, frames and shadows remain intact across opening, second row, dense Court, Resting stack and camera motion. Every public identity is reachable; labels/arrows never obscure faces; Crown indicates any surviving run, not an arbitrary dependency trio. Pan/focus/reset and reduced motion preserve causal framing. No flat dashboard substitutes for the board. |
| B607 | Build the card-first ordinary UI and direct target interactions. | Selecting a card previews Add and highlights real matching-suit rival targets; pointer/touch/keyboard can select the same target and inspect its exact identity. Add uses one deliberate commit after preview; Swap shows offer, target, possible answer ranks and destinations before commit. Incoming Answer and accepted transfer use consistent verbs. Cancel does not spend/advance; invalid or stale input cannot reveal/spend. No name-only request dropdown, irrelevant global verb list, impossible action button or terminal turn prompt. |
| B608 | Implement the prepared legal tutorial as one guide. | Execute the complete authored fixture with ordinary reducer actions; Continue changes only cursor. One exact next interaction plus Exit tutorial is available. Full names match visible cards; each new term/relation is taught before use. Origin, target and result stay in frame, including rival moves. No duplicate modal, forced reference lookup or invented resource. Count interactions/time against Build 5's roughly 75 clicks/ten minutes; fewer clicks alone do not pass comprehension. Fresh screen-only learner explains purpose before ordinary games. |
| B609 | Repair responsive layout, safe areas, focus and loading transitions. | Complete the frozen state matrix at all 21 dimensions. Required labels, full focus rings and 44px operative touch targets remain in the usable area without reducing text to force fit. Explicit prior regressions: 360×780/800 required controls, 844×390 captions, badge overlaps, excessive unused board space and off-board public piles. Same-session keyboard resize/rotation preserves typed value/caret; actual Tab/wheel reaches deliberate form controls. Test 200% zoom, synthetic insets, cold/warm/failed loading and stale async completion; never flash a white or half-loaded board. Physical-device limitations remain separately recorded. |
| B610 | Complete private local play and public knowledge presentation. | Every supported seat/first-seat configuration completes handoff without a private-card flash. Only the active recipient sees their hand; response handoff returns to the proper actor. Public remembered identities remain accurate after offer, answer, exchange, return and Add; private draws remain unknown. Show remembered cards and unknown count distinctly, without pretending every hidden identity is unknown. Public event history uses player-facing names and exact movement; tutorial hides unrelated navigation. |
| B611 | Implement finite coverage and immutable evidence joins. | Expand all action/response/tutorial/result states into literal IDs before implementation. Freeze the 21-size product with exact count/hash. Early failures leave later rows missing; no run can shrink the denominator. Source/served revision, assets, inventory, screenshot and actual review join exactly. Mismatched hashes, uninspected images, failed visual rows and downscaled-only text remain rejected/limited. Reruns preserve earlier failures. Execute the evidence gate's own deliberate mismatch/missing/duplicate tests. |
| B612 | Repair all applicable retained release failures and integrate new checks. | Reproduce/classify the seven unresolved cases; repair thirteen stale contracts without restoring rejected behavior; fix the two retained-mode defects and current layout failure. Preserve coverage for historical routes/saves. All 63 existing checks receive an explicit applicable disposition; no silent skip or waiver. New core engine/UI/tutorial/visual/evidence checks enter the complete release inventory. Rerun the entire applicable suite on an exact candidate; report timeouts/failures honestly. |
| B613 | Audit changed code, all reachable art, and every required rendered state. | Every changed source file maps to behavioral tests and receives standards/spec audit; typecheck/build/unit/UI checks pass. Every asset maps to provenance/hash and actual visual review; generated declarations are not inspection. Native/cropped readable views cover all operative text/ranks; recorded motion covers source/target/result. The finite inventory has no unreviewed required row for claimed acceptance. Findings receive written regression tasks before correction. No claim that line coverage proves correctness or agent proxies prove family enjoyment. |
| B614 | Publish the exact evaluation candidate to Prod and verify it. | Read RELEASE.md, preserve unrelated edits and Main. Commit/push only the intended package to prod, wait for source/Pages jobs, verify revision.json SHA/build plus loaded bundle identity, and operate the actual opening/tutorial/action/response/terminal at desktop and compact sizes. Report actual testable URL and build. Mandatory build/unit/UI and critical visual checks must pass; any remaining evaluation-only limitations stay explicit and do not authorize Main. |
| B615 | Complete deployed testing, fresh learning, ten games and the next gauntlet. | Run the full deployed applicable checks and actual visual/asset review on exact served code. A fresh agent receives only the URL/screens, completes tutorial and explains the experience in its own words. Observe ten complete ordinary UI games across suits, 2/3/4 players and first-seat positions; preserve terminal evidence, choices, uncertain outcomes, stalls, forced decisions, cycles and estimates. Distinguish simulations from UI play and agents from family humans. Feed results into the next panel, write all next-cycle tasks/tests before changes, and continue until interrupted. |

## Implementation order and integration

Freeze B601 and its complete contracts first. Implement B602 in red/green behavior slices, then B603/B604 through stable public boundaries. Art, renderer and UI may proceed as bounded independent work only after their specifications/tests are frozen; integrate before B608/B609/B610 interaction acceptance. B611's evidence model is part of all runs, not a last-minute reporting patch. B612 and B613 must preserve failures rather than hiding obsolete tests. B614 delivers the evaluation site; B615 closes the actual user-observed cycle.

No code from a failed experimental rule is silently retained. Current Build 5 source remains the control until the freeze; comparison probes using its public API are evidence only. No new bespoke historical powers, negotiation subsystem, external event, puzzle, network play or manufacturing certification is part of this selected iteration. The user's all-mechanics-open instruction remains active for the next review.

## Asset and code gates

Each changed production file must pass syntax/type/build checks, applicable behavior tests and a standards/spec review tied to this plan. Engine and storage tests exercise legal and rejected branches at their public boundary. Renderer and interaction branches require actual browser paths, not mock-only tests. Unexercised changed behavior is an open coverage item; remove dead behavior or add a meaningful case. Tests themselves are reviewed for independent expected results, privacy and conservation assertions, and sensitivity to the defect they claim to prevent.

Each reachable art asset must have a catalog entry: identity/use, source/reference, hash, dimensions/aspect, relevant scene states and actual reviewer verdict. Inspect original portraits, composed faces, frame fit, loading fallbacks, table/felt, Crown/markers, rank aid, wordmark and any retained-mode assets reached by the release suite. Shared assets need one intrinsic review plus all contextual screen reviews where they are used. A clean image file cannot approve a corrupted scene. New raster work requires available creative-tool discovery and applicable skills; source originals remain immutable.

For every changed ordinary rule, a fresh screen-only comprehension probe uses one short line and a visible example. Extra prose, legal filtering or memorized tutorial clicks cannot rescue failed prediction. The sixth-grade target is binding; automated reading metrics support review but cannot certify children understand the game.

## Release and evidence limits

The [coverage contract](reviews/BUILD-5-CORRECTION-COVERAGE-CONTRACT.md) defines exact dimensions, same-session keyboard tests, actual opening records and source/served joins. Its universal categories are expanded into the literal Build 6 inventory before freeze. Synthetic safe areas, browser emulation and agent play are labeled as such. Actual device/browser/OS keyboard and family-human studies remain unverified unless performed; they cannot be silently replaced by screenshots or policy runs.

Prod is the authorized evaluation destination: https://mcbradd.github.io/overlords-and-outlaws-prod/. Main remains locked. Full release acceptance is not inferred from successful deployment. Do not mark the continuous goal complete after publishing or after one successful cohort; repeat the requested cycle until the user interrupts.
