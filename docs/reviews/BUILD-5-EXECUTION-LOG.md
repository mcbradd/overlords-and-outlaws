# Build 5 execution evidence

Work in progress, 17 September 2026. Main unchanged. Baseline source `ebca68b`; complete rule/plan/test freeze committed as `3fbb734` before implementation. Existing unrelated worktree edits preserved. No Build 5 publication or blind gameplay claimed yet.

## Completed observations

- Primary agent actually opened baseline landing, intro and first tutorial table using CUA at 1280×720. Observed duplicate title, small text, centered intro, many unrelated controls, prebuilt Court and advanced History objects, tiny board elements. These captures are in the tool interaction record; they are not a final candidate visual pass.
- Independent baseline audit: 135 unit and three UI tests passed despite the visible defects.
- Three agent panels wrote systems/experience/production reviews; written-candidate cross-review fixed starter-card defense, selected-suit deck, replenishment starvation, attacker-specific target marks, foreign-ruler recovery, and rotation-before-deal. These were proposal reviews, not human professional consultation.
- Independent executable core tests were written before reducer implementation and first ran red due to the absent new engine. The initial completed reducer then passed 18 grouped tests including exhaustive suit/rank answers and a complete legal tutorial. Later AI tests brought core to 20. Separate three storage tests passed; this is 23 core/storage tests at that point, not the total repository suite.
- Actual original policy simulation: 24 games, six winners and eighteen cap draws; all eight four-seat games drew. This failed the strategic gate. The written attrition amendment specifies successful Recall exchanging its lead for the target, plus public-information policy corrections. The before/after results must remain separate.
- Primary agent opened the new landing, readable left-aligned introduction and first two-card tutorial in CUA. The initial new board still occupied too little of its host and made public names unreadable. Reopened the scene specialist's broad-board and mobile PNGs; broad composition improved but a deck/seat label overlap remained in that capture. A later correction needs its own inspected image. Mobile camera shows own Court; the rest of the public state still needs visible contextual reporting.
- Scene specialist reports four complete 52-card proof sheets actually opened, after correcting incomplete image decoding in two sheets; exact paths/inspection results are in its forthcoming scene record. These proofs do not substitute for all viewport/state checks.
- The title asset was generated and actually viewed. Two requested alpha outputs retained opaque backgrounds. Selected complete illustration uses screen blending on the dark opening; no false transparent-master claim. The primary agent opened its actual 1280×720 landing composition and verified correct lettering, no repeated title, readable card showcase and visible primary controls.
- `npm run build` passed for the initial implementation. A stable local preview at port 4173 was started to isolate browser regression from hot-reload interruptions. It predates the attrition correction and must not be mistaken for the final candidate.
- Initial browser regression produced 45 captures and passed 12 fixture/setup/error stages; HMR interrupted the tutorial. No browser gameplay defect inferred from that interruption; all captures remain uninspected until recorded otherwise.

## Outstanding delivery gates

Complete attrition correction and repeat the identical simulation comparison; audit code and rule text; execute complete repository checks; stable tutorial/browser matrix; every asset/reference proof; actual all-state viewport inspection including safe area/keyboard, keyboard/touch and reduced motion; update any unmet task before modifying it; commit/push only intended Prod work; verify deployment SHA/build and actual site; fresh screen-only tutorial followed by ten observed full games; record feedback and begin next critique/plan/test cycle.

Actual-device iOS/Android testing, manufacturing vector/print proofs and human player evidence are not claimed. Any unavailable condition remains explicit rather than being marked passed by browser emulation.

## Correction pass after the first stable preview

- The approved successful-Recall exchange changed the identical 24-policy comparison from 6 wins / 18 cap draws to 21 wins / 3 cap draws. Extended projected-policy experiments recorded 243 wins / 29 cap draws across 272 games. These remain simulations; a maximum eleven rulerless round starts in an adversarial policy case remains an agency concern. See the separate attrition results, not a claim of human fun.
- Stable-preview desktop1440×900 completed all22 scripted teaching steps and110 captures. Phone375×667 later completed110 captures without control-bound failures;390×844 completed the tutorial but incoming Trade response buttons extended12.4px below the viewport. Parent actually opened the390 Trade failure image and saw the clipped buttons. None of these automatic counts is a complete visual pass.
- Independent application audit identified denied-storage initialization, import/preload racing, timed teaching text loss, malformed tutorial progress, incorrect Trade viewpoint, and omitted full character names. APP-04 now has a red/green regression covering all44 before/after teaching boundaries and mismatch rejection. The other fixes are being separately verified; they are not closed merely because code changed.
- Written resume/response amendment preceded fixes. Incoming Trade now puts its decision before the hand on compact layouts; primary hover preserves gold background and dark-text contrast. Rival teaching uses an explicit Watch the rival move control. Continue remains cursor-only. The same22 legal actions are retained.
- The parent directly viewed the390×844 landing, left-aligned introduction and dark preparation screen in CUA. Primary title/intro controls fit and text remained readable. Hot-reload during ongoing edits prevented stable follow-through; later matrix evidence must come from the built candidate.
- A viewport amendment adds the current publicly reported leading screen dimensions to the representative matrix, for21 total. Actual-device keyboard/cutout validation remains separate. All new coverage scripts must keep generation and actual inspection records distinct.

Publication is to the evaluation branch under the standing Prod authorization. As requested by the user's loop, evaluation publication may precede the exhaustive deployed-site test cycle; any unfinished gates remain explicit. This does not relax Main's complete release and visual-inspection requirements.

## Prepublication visual correction pass

Revision2 at the stable preview completed111captures each at390×844 and375×667, with the entire legal tutorial and fixture responses. The production reviewer actually opened confirmation hover, Watch, full-name heir and Trade images: the earlier required-control clipping and dark-on-dark hover failures were corrected. The parent independently opened the375px first confirmation: guide/control text is readable, but the miniature printed face text remains much smaller than its full inspection reference. This is a compact-scale limitation, not a declaration that all card typography is comfortable.

Supplemental first-run failures and corrected harness assumptions are preserved under `artifacts/core/extended-revision2*`. Deliberate reference/form scrolling differs from rescuing a clipped action. A real landscape scroll-retention failure led to L03; an actual screenshot of the landscape logo-only first screen led to L01. The parent viewed the corrected844×390 opening and first action in CUA on dev: artwork/proposition/actions use both columns, and the single teaching guide and confirmation are visible beside board/hand. These are source-level correction observations awaiting final stable rerun.

The parent and experience reviewer opened dense1440×900 screenshots and rejected a narrow vertical board strip with unreachable later Court cards. The written D01–D04 correction retains every3Dcard while fitting a broad physical table and adds explicit focused groups with Previous/Next. Parent opened `artifacts/core/experience-revision3/1440-dense-four-courts.png` and `390-dense-focus-alba.png`: the dense overview now uses the width and the focused person/pager are readable. Whole-table miniature names are not claimed simultaneously readable; focus/inspection supplies detail. The experience reviewer owns first/last/fallback traversal checks and exact revision evidence.

The earlier complete repository run passed164unit tests,3UI tests and the production build. At11:21local the subsequent repository run actually passed165unit tests, including the new first-taught-person name regression, and3UI tests. Final scene refinements still need their stable build. No deployed Build5 or blind playtest has occurred at this record point.

## Candidate f2e1781 bounded verification

The committed implementation built successfully. Stable candidate checks passed at390×844 and375×667 (112 captures each), all16 supplemental checks and all6 application audit cases. The844×390 tutorial completed but the ordinary sparse table failed: Inspect controls spanned y378–422 beyond the390px viewport. The parent actually opened `artifacts/core/candidate-f2e1781/844x390/072-FAILED-sparse-table.png` and confirmed clipping; publication was held for a written L06 correction and rerun. These captures do not constitute the entire21-viewport visual acceptance matrix.

The parent also opened `artifacts/core/experience-revision3/final-390-marriage-notice.png` and `final-1440-incoming-recall.png`. The mobile Crown/dependency sentence and empty-hand Pass guide are readable; whole-table card details remain miniature and require focus/inspection. The desktop incoming Recall shows its source-to-target arrow, explicit target/lead text and a visible Defend card plus decline control. These are inspections of the named revision3 evidence, not a blanket final-candidate approval.

L06's rebuilt stable candidate passed112 captures and all22 tutorial actions at844×390 with zero automated failures. The production reviewer opened sparse, Recall, Trade, first defense preview and reference inspection. The parent independently opened `artifacts/core/L06-recheck/844x390/070-sparse-table.png` and `101-trade-offer.png`: Inspect and response controls fit with clearance; action labels remain readable and no build badge covers them. CSS served as `app-BsI1J21c.css` (SHA256 `10c566c80285849a1057cad61542054cb9a9a62b8780381da89b2dcb2b072171`). This is the bounded prepublication pass; exhaustive suite, deployed verification and blind games remain outstanding.
