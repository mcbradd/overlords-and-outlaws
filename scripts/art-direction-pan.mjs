import {chromium} from '@playwright/test';
const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://localhost:5182');await p.locator('[data-ui="demo"]').click();await p.locator('[data-ui="unlock"]').click();await p.locator('[data-ui="focus-0"]').click();await p.waitForTimeout(1000);console.log(await p.evaluate(()=>({viewport:innerWidth,page:document.documentElement.scrollWidth,table:document.querySelector('.h-table').scrollWidth,hand:document.querySelector('.h-card-row').scrollWidth})));
await p.locator('.h-table').evaluate(e=>e.scrollLeft=e.scrollWidth);await p.locator('.h-card-row').evaluate(e=>e.scrollLeft=e.scrollWidth);await p.screenshot({path:'artifacts/art-direction/after-phone-panned.png',fullPage:true});await b.close();
